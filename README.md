# 2025-2_Secure_SW_Implementation

이 프로젝트는 2025-2 Secure Software Implementation 수업에서 진행한 프로젝트로,
암호화 라이브러리를 활용해 파일 암호화 및 무결성 검증 기능을 제공하는 애플리케이션입니다.

🚀 주요 기능
AES-CTR 모드 암호화
사용자가 선택한 파일을 AES-CTR 모드를 사용해 안전하게 암호화합니다.

HMAC-SHA512 무결성 검증
암호화된 파일에 대해 HMAC을 생성하고, 복호화 시 이를 통해 무결성을 검증합니다.

비밀번호 기반 키 생성 (KDF + SHA-512)
사용자가 입력한 비밀번호를 기반으로 KDF를 수행한 뒤,
SHA-512 해시 결과를 가공하여 AES 마스터 키와 HMAC 키를 생성합니다.

⚙️ 기술 스택
Language: C
Crypto Library: OpenSSL
Mode: AES-CTR, HMAC-SHA512
KDF: PBKDF2-SHA512

💻 실행 버전
CLI_version1:
초기 버전으로, CLI(Command Line Interface) 환경에서만 실행이 가능한 형태입니다.
사용자는 터미널을 통해 직접 암호화 및 복호화 명령을 수행할 수 있습니다.
